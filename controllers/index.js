var model = require("../models");

module.exports = function (app) {

    // Matches with "/api/books"
    // app.route("/")
    //     .get(model.findAll)
    //     .post(model.save);

    // // Matches with "/api/books/:id"
    // app.route("/:id")
    //     .delete(model.delete);
}
